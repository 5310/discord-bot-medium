{
  "name": "discord-bot-medium",
  "version": "0.0.0",
  "description": "An versatile bot that responds to your questions through configurable microservices",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "lint": "npx tslint -p .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -Rf ./build/* && tsc",
    "start": "node -r dotenv/config .",
    "postinstall": "npm run build"
  },
  "keywords": [
    "discord",
    "bot",
    "medium",
    "microservices-application"
  ],
  "author": "Sayantan Chaudhuri",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.6.0",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5",
    "typescript-tslint-plugin": "^1.0.1"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
